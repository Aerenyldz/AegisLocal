# Copy to infra/docker/.env before starting Compose.
# Use a long, unique value outside local development.
REDIS_PASSWORD=replace-with-a-long-random-password
AEGIS_API_BIND=127.0.0.1
AEGIS_API_PORT=8000
